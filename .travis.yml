
language: python


python:
  - 3.4

sudo: false

virtualenv:

install:
  - pip install -r requirements.txt
  - pip install coveralls

before_script:
  - export PYTHONPATH=$PYTHONPATH:$(pwd)

script:
  - coverage run --source=urltheory,croawl urltheory/tests.py -v

after_success:
  - coveralls

